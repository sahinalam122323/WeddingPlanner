{
  "version": 2,
  "builds": [
    {
      "src": "wedddinnggg-laaast-finall/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/$1" }
  ]
}
